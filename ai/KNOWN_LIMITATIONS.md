# KNOWN_LIMITATIONS.md — известные ограничения

- **Нет бандлера/минификации** — всё отдаётся как есть; глобальный скоуп требует дисциплины имён.
- **Нет автотестов**, только `node --check` (синтаксис). Регрессии ловятся ревью/ручной проверкой.
- **Локализация отсутствует** — весь текст захардкожен на русском, i18n-слоя нет.
- **Вес графики полёта**: `images/fly_*.webp` ~1.3–2.1 МБ/файл (≈20 МБ) — тяжело на сотовой сети (P1).
- **Провал темпа ур.8–60** — между открытием Гнездилища и биома II нет новых систем.
- **Однобокие стоки** редких валют (⭐звёзды — только Восхождение; 🔮осколки — только «согреть»).
- **Runtime-структура плоская** (корень) ради GitHub Pages — не «идеальная» `/src`, но безопасная.
- **Нет device-QA** — производительность на реальном iPhone Safari не измерена.
