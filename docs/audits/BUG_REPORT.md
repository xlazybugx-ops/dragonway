# BUG_REPORT.md — функциональное тестирование

Статический проход по коду (ЭТАП 2). Динамических (рантайм) багов без живого прогона подтвердить
нельзя — помечено соответствующе.

## Проверки
| Проверка | Результат |
|---|---|
| Открываются ли все окна (views) | ✅ 11 секций + 2 FS-оверлея подключены к `switchView`; ветки на все `v` |
| Работают ли все кнопки | ✅ статически: обработчики навешиваются; инлайн-onclick (`switchView`,`toggleReserve`) — глобальные, определены |
| Битые ссылки на ассеты | ✅ `hub_bg.webp` и спрайты существуют; фоны полёта имеют `onerror`-fallback на процедурный фон |
| Недоступные действия | 🟡 гейтинг по уровню (`FEATURE_MIN`) корректно тостит «откроется на ур.X» — не баг, а дизайн |
| Пустые экраны | ✅ пустые состояния обработаны (логово: «Логово пустует…»; арена: «Нет бойцов…») |
| Незавершённые функции | ✅ не обнаружено заглушек-функций |
| Временные заглушки | ✅ `placeholder` — только в HTML-инпутах онбординга (легитимно) |
| TODO/FIXME | ✅ отсутствуют |
| DEBUG-код | ⚠️ один `console.log` (07-flight.js:278) |
| Неиспользуемые файлы | ⚠️ `arcade-prototype.html`, `den-prototype.html` не подключены |
| Дублирующийся код | ⚠️ `rnd`/`rand`, локальные `dist`/`clampN`; неиспользуемые `selectDragon`/`lairSorted` |
| Синтаксис всех модулей | ✅ `node --check` каждого + конкатенации в порядке загрузки — чист |

## Найденные дефекты
| ID | Описание | Серьёзность | Файл |
|---|---|---|---|
| B1 | Оставленный DEBUG `console.log` в проде | P3 | 07-flight.js:278 |
| B2 | Прототипы в релизной папке (не в игре, но отдаются) | P2 | arcade-prototype.html, den-prototype.html |
| B3 | Мёртвые функции после реворка логова (`selectDragon`, карусель) | P3 | 05-ui-base.js |
| B4 | Топбар без `safe-area-inset-top` (риск выреза) | P2 | css/style.css |
| B5 | Дубли утилит `rnd`/`rand` | P3 | 04/08b |

## Не подтверждаемо статически (нужен живой прогон)
- Фактические зависания/утечки памяти при длительной сессии.
- Поведение WebAudio при первом касании на реальном Safari.
- Корректность инкубации/сейва при фоновой выгрузке вкладки iOS.
- FPS в полёте/аркаде на устройстве.

## Вывод
Критических (P0/P1) функциональных багов в статике не найдено. Дефекты — гигиенические (P2–P3).
Обязательна живая прогонка на устройстве для подтверждения рантайм-поведения.
